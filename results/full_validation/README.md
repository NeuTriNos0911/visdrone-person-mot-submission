# Full Validation Results

This directory contains the lightweight validation artifacts kept in Git:

- `summary.json` with aggregate FPS and frame counts.
- Per-sequence JSON files with FPS and elapsed-time metrics.
- `previews/` with still frames from rendered tracking outputs.

The full rendered MP4 outputs are included in the original submission ZIP
attached to the GitHub `v1.0.0` release.
